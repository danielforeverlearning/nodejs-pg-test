<!DOCTYPE html>
<html>
<head>
  <% include ./partials/header.ejs %>
</head>
<body>
<h2>TABLE cars</h2>
<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %> - <%= r.name %></li>
    <% }); %>
</ul>
</body>
</html
