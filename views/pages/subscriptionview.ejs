<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css" />
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
<script type="text/javascript" src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>

<style>
th, td {
  border: 1px solid black;
}
</style>
</head>
<body>

<div class="container">
<h2>MONTHLY SUBSCRIPTION</h2>
    <% if (rowcount == 0) { %>
        <p>rowcount is 0</p>
    <% } else if (rowcount == 1) { %>
        <p>STUDENTID = <%= r.studentid %></p>
        <p>FIRST NAME = <%= r.firstname %></p>
        <p>LAST NAME = <%= r.lastname %></p>
        <p>EMAIL = <%= r.email %></p>
        <p><%= r.phoneareacode %></p>
        <p><%= r.phonenumber %></p>
    <% } else { %>
        <p>multiple rows, rowcount is <% rowcount %>, this should be impossible due to foreign key rule, you should never see this</p>
    <% } %>
<input type="button" onclick="window.location.href='/'" value="HOME">
</div>
</body>
</html>
